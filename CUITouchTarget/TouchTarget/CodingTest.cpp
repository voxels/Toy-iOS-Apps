//
//  CodingTest.cpp
//  
//

/**************************************************************************************
 
 Coding test:
 
 Please complete the implementation of the following CUITouchTarget as follows:
 1) When a user double taps the control, call the SendDoubleTapNotification() member
	function.
 2) A double tap is defined as follows:
	a) There is a complete cycle of two touches: Began, Ended, Began, Ended.
	b) The time between the first touch down and the last touch release is less than
	   kDoubleTapTouchTimeInterval.
	c) The user does not move the touch during the gesture.
 
**************************************************************************************/

/*
#include <stdio.h>

class CTimeClock
{
public:
	static double GetCurrentTime(); // Returns the number of seconds since January 1st, 1970
};

class CUIPoint
{
public:
	float	m_xPos;
	float	m_yPos;
};

class CUITouchTarget
{
public:
	CUITouchTarget();
	void OnTouchBegan(const CUIPoint& pos);
	void OnTouchEnded(const CUIPoint& pos);
	void SendDoubleTapNotification();
};

#define kDoubleTapTouchTimeInterval			0.500	// This is half a second.

CUITouchTarget::CUITouchTarget()
{
	
}


void CUITouchTarget::OnTouchBegan(const CUIPoint& pos)
{
	// This will be called when the user places a finger down on this control.
	// pos is the on screen position where the user touched.
}


void CUITouchTarget::OnTouchEnded(const CUIPoint& pos)
{
	// This will be called when the user releases the finger previously placed on this control.
	// pos is the on screen position where the user released.
}
*/
